server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # rightmate.zip and version.json keep their names across releases, so never cache them.
    location /download/ {
        add_header Cache-Control "no-cache";
    }
}
