node_modules/
.DS_Store
.env
backend/.env
backend/__pycache__/

YoutubeDownloader.win-x64/

# Packaged extension builds
*.zip
public/models/*.onnx
extension/inpaint/models/*.onnx
*.tsbuildinfo

# Minified extension build
dist/

# Extension signing key (decides the extension ID; never commit) and packed builds
*.pem
*.crx

# Source export of the landing page design (copied into landing/)
landing/RightMate-website-files/
landing/RightMate-website-files.zip
